# 戦艦ゲーム — TRON風 TUI / オフラインPWA 実装計画

## Context

`/Users/sogakoutarou/game/戦艦ゲーム` は空ディレクトリ（ルールブックPDFのみ配置済み）。
ハズブロ社「バトルシップ」のルールをベースにした戦艦ゲームを、
**完全オフラインで動作する PWA** として新規に作る。

- ビジュアルは TRON 系（黒地・白罫線・ネオン青／赤）、UI は TUI 相当
- 自軍=青 / 敵軍=赤、フォントは JetBrains Mono
- 対戦相手は CPU のみ（難易度3段階）
- ルールは **2エディション制**:
  - `CLASSIC` … 10×10 のクラシックルール（既定）
  - `DELUXE` … 14×10。2012年 Hasbro 映画タイアップ版の独自ルールが有効になる

ルール仕様はプロジェクト内に配置済みの公式ルールブック
`38194_en-us_deluxe-battleship-movie-edition-game.pdf`（28ページ）を一次資料として作成。
本計画の DELUXE 側の記述はすべてこのPDFの実記述に基づく。

### 法務上の前提
ゲームの**ルール自体は著作権保護の対象外**なので実装に問題はない。ただし
"BATTLESHIP" / Hasbro / Universal の名称・ロゴ・映画意匠・艦船デザインは使わない。
公式の固有艦名（Red Japanese Destroyer、Flagship、Red Storm Stinger 等）と
**音声セリフの文言**も流用せず、米海軍の船体分類記号ベースの一般名・独自の文言に置き換える。
アプリ名は独自のものにする（暫定コードネーム: **`SALVO//NET`**、変更可）。

---

## 1. 技術選定 — SvelteKit（静的SPA）

| 項目 | 選定 | 理由 |
|---|---|---|
| フレームワーク | **SvelteKit + Svelte 5 (runes)** | サーバー不要の静的SPAに最適。ランタイムが最小級でオフライン初期化が速い。runes のきめ細かいリアクティビティが最大140セル盤面の再描画に効く |
| アダプタ | `@sveltejs/adapter-static` (`fallback: 'index.html'`) | 完全静的化。ホスティング先を選ばない |
| SSR | `ssr = false`, `prerender = true` | 全処理がクライアント。オフライン要件と一致 |
| PWA | `@vite-pwa/sveltekit`（`generateSW`） | SvelteKit 公式連携。ビルド成果物を全プリキャッシュ |
| スタイル | **素の CSS + カスタムプロパティ**（Tailwind不使用） | 配色・グロー・罫線がトークン数個で表現でき、Tailwind のユーティリティは TUI 表現にほぼ寄与しない |
| フォント | `@fontsource-variable/jetbrains-mono` を**セルフホスト** | Google Fonts CDN はオフラインで死ぬ。npm 経由で bundle → SW がプリキャッシュ |
| 音響 | **Web Audio API による完全合成**（音源ファイル0バイト） | オフラインPWAに音声アセットを持ち込まずに済み、電子音は TRON/TUI の質感とも一致 |
| 描画 | **DOM (CSS Grid)**、Canvas不使用 | 最大140セル。DOM で十分軽く、キーボード操作・スクリーンリーダー・CSSグローが自然に効く |
| テスト | `vitest`（エンジン単体） + `@playwright/test`（スモーク） | ゲームエンジンを純TSに切り出して高速にテスト |
| パッケージ管理 | **npm**（pnpm/bun は未インストール） | |

**Next.js を採らない理由**: `output: 'export'` で静的化はできるが、RSC / Server Actions / ISR /
Middleware というNext固有の武器がオフラインPWAでは全て無効化され、React ランタイム分だけ重くなる。
**Astro を採らない理由**: 本作は画面全体が単一の巨大インタラクティブ島であり、Astro のゼロJS戦略の恩恵が出ない。

---

## 2. 艦船・ピンの表現

### 2.1 描画レイヤー

各セルは固定サイズのボックス。艦は以下の3層で描画する。

```
層1 塗り     艦首セル = 進行方向の三角グリフ（◀ ▶ ▲ ▼）
             それ以外 = ▓
             ▶ 色は所属で決まる:  自軍 = ネオン青 #00E5FF / 敵軍 = ネオン赤 #FF2E4C

層2 艦種コード   塗りの上に白文字(#FFFFFF)でオーバーレイ
             ▶ CV/BB/DD/DE : 艦首を除く各セルにコードを配置
             ▶ PB のみ例外 : 全長2マスで艦首を除くと1マスしか残らないため、
                            艦首の三角グリフを含む2マス全体にまたがって
                            中央に 'PB' を1つだけオーバーレイする

層3 ピン     外れ = ● 白 / 命中 = ● 赤 + ╳ 赤（さらに上に重ねる）
             SCAN = ● 青（公式の青ペグ準拠。§2.4）

罫線         セル境界は CSS border（白）。艦の外周のみ所属色でハイライト
```

層2・層3はセルの grid に対して `position: absolute` の別レイヤーとして重ねる。
PB のオーバーレイだけは2セル分の幅（または高さ）を持つ単一要素として艦の中心に配置し、
`pointer-events: none` にして下のセルのクリック判定を殺さないようにする。

### 2.2 艦種コード

| コード | 艦種 | 長さ | 公式ルールブック上の対応艦（Navy / Alien） |
|---|---|---|---|
| `CV` | Aircraft Carrier | 5 | Aircraft Carrier / Flagship |
| `BB` | Battleship | 4 | Battleship / Heavy Ordnance Craft |
| `DD` | Destroyer | 3 | Red Japanese Destroyer / Red Storm Stinger |
| `DE` | Destroyer Escort | 3 | Blue American Destroyer / Blue Shredder Stinger |
| `PB` | Patrol Boat | 2 | RHIB / Small Assault Ship |

公式は3マス艦を色（Red / Blue）で区別するが、自軍=青・敵軍=赤という本作の配色規則と
正面衝突するため、実在する米海軍の船体分類記号 `DD` / `DE` に置き換える。
ソナーを持つのが `DE`（護衛駆逐艦）なのは対潜艦の役割とも一致する。

### 2.3 描画例（自軍・青）

塗り（`▲ ◀ ▓`）は青、艦種コードは白。

```
    1     2     3     4     5     6
A   ·     ·     ·   [ ▲ ]   ·     ·      ← BB 艦首（北向き）
B   ·     ·     ·   [▓BB]   ·     ·
C [ ◀ ] [▓CV] [▓CV] [▓BB]   ·     ·      ← CV は西向き（艦首が左端）
D   ·     ·     ·   [▓BB]   ·     ·
E   ·     ·   [ ▼ ] [ · ]   ·     ·      ┐ PB（南向き・全長2）
F   ·     ·   [ ▓ ] [ · ]   ·     ·      ┘ 2マスにまたがって 'PB' を中央に1つ

               └─ P B ─┘                 ← 白文字オーバーレイ（2セル分を1要素で描画）
```

被弾・撃沈の状態遷移:

```
無傷   [▓CV]   塗り=所属色 / コード=白
被弾   [●╳ ]   該当セルのみ 赤ピン ● + 赤 ╳ を重ねる
撃沈   [▒╳ ]   艦全体をディム（暗赤 #8B1024）+ 外周を赤の破線に
```

### 2.4 ピン

| ピン | 表現 | 意味 |
|---|---|---|
| MISS | `●` 白 `#FFFFFF` | 外れ |
| HIT | `●` 赤 `#FF2E4C` + `╳` 赤を重ねる | 命中（`●` は下層に残す）+ 一瞬のグローパルス |
| SCAN | `●` 青 `#00E5FF` | SCAN の中心座標（公式の青ペグ準拠） |

**青の衝突対策**: 敵盤面には自軍艦が描かれないため、敵盤面上の青は SCAN ピンだけになる。
加えて、艦の青は**セル全体の塗り**、SCAN の青は**小さな点**と形状が異なる。
凡例（`┌ LEGEND ┐`）を常設して取り違えを防ぐ。

敵盤面は撃沈するまで艦を隠す。撃沈時に初めて艦全体の塗り（三角＋▓）を**ネオン赤**で、
艦種コードを白で開示する。

### 2.5 艦種コード表（常時表示）
戦闘中も常に参照できるよう、`┌ HULL CODES ┐` パネルを盤面脇（モバイルでは折りたたみ可能な
下部パネル）に常設する。列は「コード / 艦種名 / 全長 / 特殊兵器の残弾 / 残存状態」。

### 2.6 配置時の回転（テトリス方式）
艦は**艦首セルを軸(pivot)**に持ち、`facing: 'N'|'E'|'S'|'W'` の4方向を取る。
船体は艦首から後方へ `length-1` セル伸びる。

- `R` / 右クリック / 回転ボタン → 時計回りに `N→E→S→W` を巡回（艦首セルは固定）
- 回転結果が盤外または他艦と衝突する場合、**ウォールキック**を試行:
  艦軸方向へ `±1 … ±(length-1)` セルずらして最初に成立した位置を採用
- どれも成立しなければ回転を棄却し、艦を赤くフラッシュ（`prefers-reduced-motion` 時はフラッシュ無し）
- 移動: 矢印キー / ドラッグ、確定: `Enter`、全艦ランダム配置: `Shift+R`

---

## 3. ルール仕様

### 3.1 エディション選択（最上位の設定）

| エディション | 盤面 | 内容 |
|---|---|---|
| `CLASSIC`（既定） | **10×10**（A–J × 1–10） | クラシックの公式ルール。1ターン1射撃。ハウスルールのトグルのみ |
| `DELUXE` | **14×10**（A–J × 1–14） | 映画タイアップ版の独自ルールが有効化される（§3.2〜3.9） |

DELUXE の 14列は公式ルールブックの配置フォーメーション図（p.18–27、列見出しが 1–14）と
図15の射撃例「target coordinate J14」で確認済み。
盤面は 140マスに対し艦は17マス（密度12%）で、クラシック（17%）より薄い。
Advanced Weapons の広範囲攻撃が成立するのはこの密度が前提になっている。

**列ラベル**: 公式の Naval Letter Code に従い、行 A–J をフォネティックコード
（Alpha / Bravo / Charlie / Delta / Echo / Foxtrot / Golf / Hotel / India / Juliet）で
併記する。TUI の情報密度と相性が良い。

### 3.2 配置ルール（両エディション共通）

- 艦は縦または横のみ。**斜め配置は不可**
- 各セルは固有の座標を持つ（＝艦同士の重複不可）
- 公式ルールブックには「艦体が盤外にはみ出したり他艦に重なってもよい」とあるが、これは
  立体模型の船体形状に関する記述で、続けて「**各ターゲットホールが固有の座標を持つこと**」と
  条件づけられている。2Dグリッド実装では通常の重複禁止に帰着するため、そのように実装する

### 3.3 ゲームタイプ（DELUXE / 公式の3種）

| タイプ | 内容 |
|---|---|
| `CLASSIC GAME` | 1ターン1射撃。最初に敵艦隊を全滅させた側の勝ち |
| `MULTI-ATTACK` | Classic と同じだが、**敵艦に命中したとき、または SCAN で探知したとき**に追加ターンを得る |
| `SALVO` | 自艦隊の**残存艦数**ぶん射撃する。開始時5発、自艦が沈むごとに1発減る |

### 3.4 兵装選択（DELUXE / 公式の2種）

- `BASIC WEAPONS` … 1回に1発。通常射撃のみ
- `ADVANCED WEAPONS` … 自分のターンに「通常射撃1発」**または**「特殊兵器を1つ起動」を選べる

### 3.5 ADVANCED WEAPONS（艦種ロック式・公式準拠）

**すべての特殊兵器は、その艦が浮いている間しか使えない。**

| 艦 | 兵器 | 回数 | 効果 |
|---|---|---|---|
| `BB` | ミサイル | **1ゲーム1回** | 中心座標を指定し、**3×3 の9マス**を一斉解決 |
| `DD` | ミサイル | **2回**（別ターン） | 水平／垂直を選び、**直線3マス**の中心座標を指定して一斉解決 |
| `DE` | 追尾ミサイル | **2回**（別ターン） | 盤面**端**の座標から直線に進み、**最初に当たった艦で停止**。手前は全て外れ |
| `DE` | ソナー | **無制限・毎ターン可** | 中心座標の **3×3 を探知**。返るのは「敵艦を探知／クリア」の**二値のみ**（座標も隻数も分からない）。中心に青ピンを置く |
| `CV` | 艦載機 ×2 | §3.6 | 走査と一度きりの攻撃 |
| 艦隊共通 | 対空ミサイル | 制限なし | 自分の海面に浮いている**敵の艦載機**の座標を推測して撃つ。命中すれば撃墜 |
| `PB` | — | — | 特殊兵器なし |

> ご質問の「戦艦の特殊兵器としてのミサイル」は、独自追加ではなく**公式ルールに元から存在**した。
> `BB` の 3×3 ミサイル（1ゲーム1回・BB生存中のみ）がそれにあたる。

### 3.6 艦載機（CV / 公式準拠）

**配備**: 配置フェーズで、2機を**空母の任意のセル上**に搭載する（座標を占有する）。

**発進**: 敵盤面の任意座標を中心に配置し、走査パターンを選ぶ。どちらも中心＋隣接4マスの計5マス。

```
   X型 (pattern 1)        十字型 (pattern 2)
    ●  ·  ●                ·  ●  ·
    ·  ◉  ·                ●  ◉  ●
    ●  ·  ●                ·  ●  ·
```

**走査と攻撃**:
- 弾薬がある状態で走査して目標を発見 → **発見した全目標に命中**（赤ピン）。
  走査した残りのマスは外れ（白ピン）。ここで**弾薬を消費**する
- 目標が無ければ走査5マスすべてが外れ（白ピン）
- 弾薬切れ後は**走査のみ**可能。艦の有無は報告するが被弾は発生させない
  （※「out of ammo and can only scan」の解釈。プレイテストで調整）

**移動**: 発進済みの機体は以降のターンに別座標へ移動できる。

**撃墜**: 次のいずれかで機体を失う。
- 未発進のまま、空母上の**その機体が載っているセル**を撃たれた
- 敵の対空ミサイルに撃墜された

### 3.7 情報開示ルール（公式準拠）

- 命中を告げるが、**どの艦に当たったかは伝えない**
- 艦の全セルが被弾した時点で撃沈となり、**そのとき初めて艦名（艦種）が公表される**
- ソナーは「探知した／クリア」の二値のみ。位置も隻数も返さない

### 3.8 HOUSE RULES（CLASSIC エディション用のトグル / 非公式）
| 名称 | 内容 |
|---|---|
| `BONUS TURN` | 命中したらもう一度撃てる（DELUXE の `MULTI-ATTACK` の簡易版） |
| `NO ADJACENCY` | 配置時、艦同士の8近傍接触を禁止 |
| `SALVO` | 残存艦数ぶん射撃（DELUXE の同名タイプを 10×10 に持ち込む） |
| `SUNK SILENCE` | 撃沈の通知をしない（公式の §3.7 と矛盾するため DELUXE では選択不可） |
| `MOBILE FLEET` | 1ターン1隻だけ1マス前進/後退できる（※フェーズ2扱い。AIの確率密度モデルへの影響が大きいため後回し） |

### 3.9 定型配置フォーメーション（100種 / 公式 p.18–27）

公式の Fleet Deployment Options に対応する2方式を用意する。

- `CUSTOM` … 手動配置（§2.6 のテトリス方式）
- `PRESET` … `A1`–`J10` の100種から選ぶ。コード直接入力とミニプレビュー付きピッカーの両対応

図はすべて 14×10 なので **`PRESET` は DELUXE 専用**（CLASSIC では選択肢に出さない）。

各図は5隻＋艦載機2機を色分けで示しており、**艦種・座標・艦首の向きがすべて読み取れる**。
p.18 の `A1` を高解像度で確認した結果:

```
A1:  DE B4–B6(西)  BB E10–E13(西)  CV G7–G11(西, 艦載機 G8/G10)
     PB G14–H14(北)  DD I2–I4(西)          計17マス / 5隻
```

読み取り規則:

| 図中の表現 | 意味 |
|---|---|
| 赤い艦体（3マス） | `DD` |
| 青い艦体（3マス） | `DE` |
| 灰色の艦体 | `CV`(5) / `BB`(4) / `PB`(2) — 長さで判別 |
| 5マス艦上の赤/青の三角 | 艦載機2機の搭載位置 |
| 艦体の尖った側 | 艦首（`facing` に直結） |

CPU 側は既定で独自のランダム配置を使う（人間が100種を暗記して有利になるのを避ける）。
`PRESET` からの抽選はオプションとして提供する。

---

## 4. AI（難易度3段階 / 公式の Level 1–3 に対応）

| 難易度 | アルゴリズム | 10×10 での期待手数 |
|---|---|---|
| `LEVEL 1 / ENSIGN` | 完全ランダム（既射撃セルのみ除外） | ~95手 |
| `LEVEL 2 / CAPTAIN` | Hunt / Target: 命中後は上下左右を優先、2連続命中で軸を確定して延長 | ~65手 |
| `LEVEL 3 / ADMIRAL` | **確率密度マップ** + パリティ探索。残存艦の全配置パターンを走査してセルごとの出現頻度を集計し最大値を撃つ。命中中は制約付き（既知ヒットを必ず含む配置のみを集計） | ~42手 |

`NO ADJACENCY` 有効時は配置列挙に隣接制約を掛けて密度精度を上げる。
14×10 では探索空間が広がるため、密度マップは**残存艦種ごとの配置列挙をインクリメンタルに
更新**（毎ターン全列挙し直さない）してフレーム落ちを避ける。

**ADVANCED WEAPONS 使用時の判断**（CPU も人間と同じ残弾制約で運用する）
- `BB` 3×3 ミサイル … 3×3 窓の密度合計が最大の中心を選ぶ。1回きりなので、
  密度の山が十分に高くなるまで温存する（閾値方式）
- `DD` 直線3マス … 水平/垂直それぞれの3連窓で密度合計最大を選ぶ。命中軸が判明していれば
  その軸に沿って撃つ
- `DE` 追尾ミサイル … 密度合計が最大の行/列を、密度の重心に近い側の端から撃つ
- `DE` ソナー … 無制限なので、密度分布の**エントロピーが最大**の 3×3 を毎ターン走査し、
  結果（二値）を密度マップの事前分布に反映する
- `CV` 艦載機 … 未走査領域の密度が高い場所へ移動しつつ、弾薬は「5マス中2マス以上が
  高密度」になるまで温存する
- 対空ミサイル … 敵機の直近の攻撃座標から推定範囲を絞る（公式のヒントと同じ戦略）

---

## 5. 音響とランプ演出（電子ユニットの代替）

実物は音声プロンプト・効果音・ランプで進行を伝える。本作は
**Web Audio API による完全合成**でこれを置き換える。音源ファイルは一切持たない
（オフラインPWAにアセットを増やさず、電子音は TRON/TUI の質感とも合う）。

### 5.1 合成エンジン

`src/lib/audio/synth.ts`
- `AudioContext` は遅延生成し、**最初のユーザー操作で `resume()`**（ブラウザの自動再生制限対策）
- マスター `GainNode` を経由。ミュートと音量は `localStorage` に保存、`M` キーでトグル
- プリミティブは2つだけ: `tone({freq, type, dur, attack, decay, sweepTo})` と
  `noise({dur, filter, sweepTo})`。すべての効果音はこの組み合わせで作る
- エンジンは解決結果として `GameEvent[]` を返す。**音・ランプ・戦闘ログはこの同一イベント列を
  購読する**（分岐ロジックを三重に書かない）

### 5.2 効果音の対応表

| 公式ユニットの挙動 | 本作の合成音 |
|---|---|
| 起動音声 | `boot`: のこぎり波 220→880Hz スイープ + ノイズの立ち上がり |
| 「手番の告知」 | `turn_start`: 矩形波 660→880Hz の2音ブリップ（各40ms） |
| カーソル移動 | `cursor`: 矩形波 880Hz / 20ms の極短ブリップ |
| FIRE | `fire`: ホワイトノイズを 3000→300Hz へ掃引するバンドパスに通す |
| 命中（爆発音＋**赤ランプ**） | `hit`: ローパスノイズバースト + サイン 160→40Hz 下降 |
| 外れ（ブザー＋**黄ランプ**） | `miss`: 帯域ノイズの短い水しぶき + 矩形波 110Hz を120ms |
| ソナー | `sonar`: サイン 1200Hz を長い指数減衰で。**探知時は300ms後に第2ピング**を返す |
| 撃沈 | `sunk`: のこぎり波 400→60Hz のグリッサンド + ノイズの尾 |
| 艦載機発進 | `launch`: 三角波 200→1400Hz の上昇スイープ |
| 撃墜 | `shootdown`: 短い上昇の直後に急落 |
| 無効操作 | `invalid`: 矩形波 150Hz のブザー2連 |
| 勝利 / 敗北 | `victory`: 完全5度の上昇アルペジオ / `defeat`: 下降 |

### 5.3 音声プロンプトの扱い

公式の音声セリフは著作物なので**文言は流用しない**。機能スロット（手番告知・命中報告・
撃沈報告・探知結果）だけ踏襲し、本作では**戦闘ログのTUIテキスト**として独自の文言で表示する。
任意で Web Speech API (`speechSynthesis`) による読み上げをオプション提供するが、
**オフライン時に音声エンジンが使えない環境がある**ため既定オフ・失敗時は黙ってログのみに
フォールバックする（フォールバックの存在自体をテストで担保する）。

### 5.4 ランプ

`StatusLamps.svelte` — 実機のコントロールパネル相当のランプ列を TUI の枠内に置く。

- **赤ランプ = 命中**、**黄ランプ `#FFB300` = 外れ**（公式ユニット準拠）
- 黄は盤面の4色規則（黒/白/青/赤）の外側にある「筐体クローム」として扱うため追加を許容する。
  盤面のピンには一切使わない
- `prefers-reduced-motion: reduce` では点滅させず、点灯状態の切替のみ行う

### 5.5 アクセシビリティ
音を切っても情報が欠落しないこと（ログ・ランプ・盤面のグリフで完結する）を要件とする。
音量0でプレイスルーできることを結合テストで確認する。

---

## 6. フォーメーション抽出パイプライン（一度だけ実行）

100種の定型配置は PDF の**ベクタ図**なのでテキスト抽出では取れない。
`scripts/extract-formations.ts` を書き、**一度だけ実行して生成物をコミット**する
（ビルド時に PDF へ依存させない）。

1. `pdftocairo -png -r 600 -f 18 -l 27` で10ページを描画（各ページ 2列×5行 = 10配置）
2. 各パネルを外枠の濃色矩形から検出する（ページ内座標をハードコードしない）
3. パネル内部を 14×10 に等分し、各セル中心の色を分類
   （teal=空 / 灰=汎用艦 / 赤=`DD` / 青=`DE` / 明るい赤三角=赤機 / 明るい青三角=青機）
4. 同種の連続セルを艦にまとめ、長さ 5/4/2 で `CV`/`BB`/`PB` を確定
5. 艦軸方向の両端の外側画素を見て、**艦体色が外へ長く伸びている側を艦首**として `facing` を決定
6. **全100件をバリデーション**: 5隻ちょうど / 長さ集合が {5,4,3,3,2} / 赤3と青3が各1隻 /
   重複なし / 全て盤内 / 艦載機2機がともに5マス艦の上。
   1件でも失敗したら該当IDを標準エラーに出し、描画PNGと突き合わせて手作業で補正する
7. `src/lib/data/formations.ts` に型付きテーブルとして出力（キー `A1`–`J10`）

**検算**: `A1` は上の §3.9 に記した内容と一致すること（抽出器の正しさの基準ケースにする）。

データ量は1配置あたり5隻＋2機ぶんの座標のみ。短い文字列にエンコードすれば全体で数KB程度で、
プリキャッシュ対象に含めても問題ない。

---

## 7. ディレクトリ構成

```
scripts/
  extract-formations.ts    # §6。一度だけ実行して src/lib/data/formations.ts を生成
src/
  lib/
    engine/                  # 純TS・フレームワーク非依存（全部 vitest でテスト）
      types.ts               # Coord, Facing, ShipClass, CellState, Board, GameState, GameEvent
      edition.ts             # CLASSIC(10x10) / DELUXE(14x10) の盤面・ルール定義
      fleet.ts               # 艦定義（CV5/BB4/DD3/DE3/PB2）と艦種コード・兵装枠
      geometry.ts            # cellsOf(ship) / rotate(facing) / wallKick() / 各種射撃パターン
      placement.ts           # 配置検証・衝突・隣接制約・ランダム配置
      ruleset.ts             # RuleSet 型 + プリセット（ゲームタイプ / 兵装 / ハウスルール）
      weapons/
        index.ts             # 兵器の使用可否判定（艦の生存・残弾）とディスパッチ
        patterns.ts          # 3x3 / 直線3 / 追尾直線 / X型 / 十字型 の射撃セル集合を生成
        sonar.ts             # 3x3 二値探知
        aircraft.ts          # 艦載機の状態機械（搭載/発進/移動/弾薬/撃墜）
        antiair.ts           # 対空ミサイル
      resolve.ts             # fireAt / applyShots / 撃沈判定 / 追加ターン判定 / 勝敗判定
      rng.ts                 # mulberry32 シード付きPRNG（テスト再現性のため）
      ai/
        index.ts             # 難易度ディスパッチ
        hunt.ts              # Hunt/Target
        density.ts           # 確率密度 + パリティ + ソナー結果の事前分布反映
        weaponChoice.ts      # 特殊兵器の期待利得評価
      persist.ts             # localStorage へのスナップショット保存/復元
      *.test.ts
    data/
      formations.ts          # §6 の生成物。100種の定型配置（A1–J10）
    audio/
      synth.ts               # Web Audio 合成エンジン（tone / noise プリミティブ）
      sounds.ts              # SoundId と各効果音の定義（§5.2）
      speech.ts              # 任意の読み上げ。既定オフ・失敗時はログのみ
    state/
      game.svelte.ts         # runes によるゲーム状態コンテナ（エンジンの薄いラッパ）
    ui/
      tokens.css             # TRONパレット・グロー・タイポグラフィ
      Panel.svelte           # ┌─ TITLE ─┐ 形式の枠
      Grid.svelte            # 盤面。セル層 / コード層 / ピン層 / 艦載機層を重ねる
      Cell.svelte            # 塗り（艦首三角 or ▓）+ ピン（● / ●+╳ / 青●）
      ShipCodeLayer.svelte   # 白文字の艦種コードオーバーレイ。PB の2セル横断もここで処理
      AircraftLayer.svelte   # 盤面上に浮く艦載機マーカーと走査パターンの表示
      HullCodes.svelte       # ┌ HULL CODES ┐ 艦種コード表 + 特殊兵器の残弾
      Legend.svelte          # ┌ LEGEND ┐ ピンの凡例（白/赤/青の取り違え防止）
      StatusLamps.svelte     # 赤（命中）/ 黄（外れ）ランプ列
      FleetStatus.svelte     # 艦隊状況（残存/被弾数バー）
      WeaponBar.svelte       # ┌ WEAPONS ┐ 起動可能な特殊兵器の一覧
      Targeting.svelte       # 3x3 / 直線3 / 追尾線 / 走査パターンの照準プレビュー
      FormationPicker.svelte # A1–J10 のミニプレビュー付きピッカー
      LogPanel.svelte        # 戦闘ログ（TUI風スクロール）
      Keycaps.svelte         # 下部キーバインドヒント
  routes/
    +layout.ts               # ssr=false / prerender=true
    +layout.svelte           # フォント・トークン読み込み・全体フレーム
    +page.svelte             # 単一ルート。phase ステートマシンで画面遷移
static/
  manifest.webmanifest, icons/（192/512/maskable）
```

**単一ルート + フェーズ機構**にする（`TITLE → CONFIG → DEPLOY → BATTLE → RESULT`）。
ルート分割するとゲーム状態の持ち回りが煩雑になるため。

---

## 8. デザイントークン

```css
:root {
  --bg:          #000000;
  --rule:        #FFFFFF;        /* 罫線 */
  --rule-dim:    rgba(255,255,255,.35);
  --ally:        #00E5FF;        /* 自軍ネオン青（艦の塗り = ▓ と艦首三角 / SCANピン） */
  --ally-glow:   rgba(0,229,255,.45);
  --enemy:       #FF2E4C;        /* 敵軍ネオン赤（艦の塗り / 命中ピン / ╳ / 命中ランプ） */
  --enemy-glow:  rgba(255,46,76,.45);
  --code:        #FFFFFF;        /* 艦種コードのオーバーレイ文字 */
  --lamp-miss:   #FFB300;        /* 外れランプ（筐体クローム専用。盤面には使わない） */
  --sunk:        #8B1024;
  --font-mono:   'JetBrains Mono Variable', ui-monospace, monospace;
}
```

- グローは `text-shadow` / `box-shadow` の2段（近接+拡散）で TRON のブルーム感を出す
- 枠は CSS border で組む（文字グリッドの桁ズレを避ける）。`┌─ TITLE ─┐` 風の見出しは
  border + 背景を切り抜いたラベルで再現する
- **色だけに情報を載せない**: 外れ `●` / 命中 `●`+`╳` / SCAN `●`（青・小径）と
  グリフと大きさでも区別し、`LEGEND` を常設する
- `prefers-reduced-motion: reduce` でスキャンライン・パルス・フラッシュを無効化

### レイアウト
- デスクトップ: 敵盤面（左・赤）／自盤面（右・青）を横並び、
  下段に FLEET / HULL CODES / WEAPONS / LEGEND / LAMPS / LOG
- **14×10 のとき**は横幅が足りなければ盤面を縦積みに切り替える
  （セルサイズと盤面アスペクト比からコンテナクエリで自動判定）
- モバイル (<900px): 盤面をタブ切り替え、`ENEMY` / `FLEET` の2タブ。
  HULL CODES と LEGEND は下部の折りたたみパネル（既定は開）

### キーバインド（TUI準拠・全操作キーボードで完結）
`hjkl` / 矢印 = カーソル、`Enter`/`Space` = 射撃・確定、`R` = 回転、`Shift+R` = ランダム配置、
`1`–`5` = 特殊兵器の起動（艦種順）、`P` = 走査/射撃パターンの切替、`Esc` = 照準キャンセル、
`M` = ミュート、`Tab` = パネル移動、`?` = ヘルプ

---

## 9. PWA / オフライン

- `@vite-pwa/sveltekit` の `generateSW`、`registerType: 'autoUpdate'`
- `globPatterns` に `**/*.{js,css,html,woff2,svg,png,webmanifest}` を含め、
  **JetBrains Mono の woff2 とアイコンを確実にプリキャッシュ**
- **音源ファイルは存在しない**（全て Web Audio 合成）ためキャッシュ対象に音声アセットは無い
- `navigateFallback: '/index.html'`（adapter-static の fallback と対応）
- ネットワークアクセスは一切なし（外部CDN・解析・フォント取得すべて禁止）
- 進行中の対局は `localStorage` にスナップショット保存し、再訪時に `RESUME` を提示
- manifest: `display: 'standalone'`, `background_color/theme_color: '#000000'`, `orientation: 'any'`

---

## 10. 実装順序

1. **足場**: `npx sv create .`（minimal / TypeScript）→ adapter-static / vite-pwa / fontsource /
   vitest 導入、`ssr=false` 設定
2. **エンジン中核**: `types`（`GameEvent` を含む）→ `edition`（盤面サイズを引数化）→ `fleet` →
   `geometry`（回転・ウォールキック）→ `placement` → `resolve`。ここまで vitest で固める
3. **UI骨格**: tokens.css / Panel / Grid / Cell / ShipCodeLayer / HullCodes / Legend。
   CLASSIC エディション（10×10・1ターン1射撃）で一通り遊べる状態にする
4. **音響とランプ**: `synth` → `sounds` → `StatusLamps`。`GameEvent` 列を購読する形で接続し、
   この時点で「撃つ→鳴る→光る→ログに出る」を一本の経路にしておく
5. **AI**: LEVEL1 → LEVEL2 → LEVEL3 の順。密度マップは配置列挙のテストを先に書く
6. **ハウスルール**: `ruleset.ts` のフラグとして BONUS TURN / NO ADJACENCY / SALVO /
   SUNK SILENCE を追加
7. **DELUXE エディション**: 14×10 盤面、3ゲームタイプ（CLASSIC / MULTI-ATTACK / SALVO）、
   兵装選択（BASIC / ADVANCED）、フォネティックコード表示
8. **ADVANCED WEAPONS（艦載機を除く）**: `patterns` → `BB` 3×3 → `DD` 直線3 →
   `DE` 追尾ミサイル → `DE` ソナー。照準UIと残弾管理、AI の兵器選択まで
9. **艦載機 + 対空ミサイル**: 搭載・発進・走査パターン・弾薬・移動・撃墜の状態機械。
   盤面に浮くレイヤーと AI の運用判断
10. **定型配置フォーメーション**: §6 の抽出スクリプトを実行して `formations.ts` を生成し、
    `FormationPicker` と `PRESET` 配置フローを実装
11. **PWA仕上げ**: manifest / アイコン / SW / セーブ復元 / Lighthouse 対応
12. **磨き込み**: グロー・スキャンライン・命中演出、14×10 のレイアウト切替、a11y、reduced-motion

`MOBILE FLEET` は 12 の後に単独で扱う（AI への影響が大きいため独立フェーズ）。

---

## 11. 検証

**単体（vitest）**
- `geometry`: 4方向の占有セル、回転の巡回、ウォールキックの成立/棄却
- `edition`: CLASSIC=10×10 / DELUXE=14×10 で座標系・ラベル（A–J × 1–14）が正しいこと
- `placement`: 盤外・重複・斜め禁止・隣接制約（`NO ADJACENCY` on/off の両方）
- `resolve`: 命中/外れ、撃沈判定、勝利判定、
  - `MULTI-ATTACK` が**命中時とソナー探知時の両方**で追加ターンを与えること
  - `SALVO` が「開始5発・自艦撃沈ごとに-1発」で推移すること
  - 命中時に艦種を伝えず、撃沈時にのみ艦名を公表すること
  - 1回の解決が**過不足のない `GameEvent` 列**を返すこと（音・ランプ・ログの唯一の入力源）
- `weapons/patterns`: 3×3 / 直線3（水平・垂直）/ X型 / 十字型 のセル集合。
  **盤外にはみ出す分は無視**され、端で撃っても不正にならないこと
- `weapons`: 残弾（BB×1 / DD×2 / DE追尾×2 / ソナー無制限）と、
  **その艦が沈むと即座に使用不可**になること
- `weapons/sonar`: 返り値が二値のみで、座標も隻数も漏れないこと
- 追尾ミサイル: 端から進んで**最初の艦で停止**し、手前が全て外れになること。
  何も無ければ線上すべてが外れになること
- `weapons/aircraft`:
  - 配備時に空母のセルを占有すること
  - 弾薬ありで目標発見 → 発見マスが命中・残りが外れ・弾薬消費
  - 弾薬切れ後は走査のみで被弾が発生しないこと
  - 未発進のまま搭載セルを撃たれると機体を失うこと
  - 対空ミサイル命中で撃墜されること
- `data/formations`: **100件ちょうど**存在し、キーが `A1`–`J10` を網羅すること。
  全件が「5隻 / 長さ集合 {5,4,3,3,2} / `DD`と`DE`が各1隻 / 重複なし / 盤内 /
  艦載機2機が5マス艦上」を満たすこと。**`A1` が §3.9 の期待値と一致**すること
- `audio/synth`: `AudioContext` を**モジュール読み込み時に生成しない**こと、
  ミュート時に `GainNode` の出力が 0 になること、
  `speech` が利用不可環境で例外を投げずログのみにフォールバックすること
- `ai/density`: 制約付き配置列挙が既知ヒットを必ず含むこと、既射撃セルを再選択しないこと、
  ソナーの二値結果が事前分布に反映されること、沈んだ艦の兵器を選ばないこと
- 統計テスト: LEVEL3 を1000局セルフプレイさせ、10×10 で平均手数が 40–50 手に収まること。
  14×10 でも決着すること（手数の基準値はここで取得して以後の回帰基準にする）

**結合（Playwright スモーク）**
- CLASSIC: 配置 →（キーボードのみで）射撃 → 勝利まで到達できること
- DELUXE + ADVANCED: `BB` の 3×3 プレビューが出て、プレイ後に9マス分の結果が反映されること。
  艦載機を発進 → 走査パターン切替 → 5マスが解決されること
- `PRESET` で `A1` を選ぶと §3.9 のとおりに5隻＋艦載機2機が配置されること
- **音量0（ミュート）で最初から最後までプレイできる**こと

**手動/ツール検証**
1. `npm run dev` → ブラウザMCPで各フェーズのスクリーンショットを取り、以下を目視確認
   - 艦の塗り（三角＋▓）が自軍=青／敵軍=赤になっている
   - 艦種コードが白文字でオーバーレイされ、**PB だけ2セルにまたがって1つ**表示される
   - 4方向すべてで艦首三角が正しい向きを指し、回転・ウォールキックが破綻しない
   - 命中セルに `●`（赤）と `╳`（赤）が重なる／外れは白 `●`／SCAN は青 `●`
   - HULL CODES と LEGEND が戦闘中も常時見えている
   - ランプが命中=赤／外れ=黄で点灯する
   - DELUXE で 14×10 が破綻せず、狭い幅では盤面が縦積みに切り替わる
   - `FormationPicker` のミニプレビューが PDF の図と一致する（抜き取りで数件）
2. 音を出して、命中・外れ・ソナー・撃沈・発進・撃墜が**互いに聞き分けられる**ことを確認
3. `npm run build && npm run preview` → DevTools の **Offline** をオンにしてリロードし、
   完全に起動・プレイ可能なことを確認（フォントのフォールバックが起きていないこと、
   合成音が鳴ることも確認）
4. Lighthouse の PWA / Accessibility / Performance を実行（Installable、コントラスト比を確認）
5. モバイル幅（375px）とデスクトップ幅で表示崩れがないこと
6. 対局中にリロードして `RESUME` が正しく復元されること

---

## 12. 出典

**一次資料**: `38194_en-us_deluxe-battleship-movie-edition-game.pdf`
（Deluxe BATTLESHIP Movie Edition 公式ルールブック / Hasbro, 2012 / 28ページ）
— プロジェクトディレクトリに配置済み。DELUXE 側の全ルールはこのPDFの記述に基づく。
主要な参照箇所: p.3（ペグ構成）、p.4（両艦隊と全長）、p.6–7（ゲームタイプ / 兵装選択 /
難易度 / 配置オプション）、p.8（配置ルール）、p.10–11（基本ルールと Naval Letter Code）、
p.11–17（Advanced Weapons 各種）、p.18–27（配置フォーメーション100種。盤面が 14×10 である根拠）。

**未反映の要素**: なし。電子ユニットの音声・効果音・ランプは §5 で Web Audio 合成と
ランプ表示に置き換え、100種の定型配置フォーメーションは §6 で抽出して取り込む。
公式の音声セリフの文言のみ、著作物のため独自表現に差し替える。

> ゲームのルール自体は著作権の保護対象外のため実装に問題はない。
> "BATTLESHIP" / Hasbro / Universal の名称・ロゴ・映画意匠、公式の固有艦名、
> および音声セリフの文言は使用しない。
